import React from 'react'

export const PageCart = (props) => {
    const { order
    } = props;
    return (
        <div>
            {order.map((item) => {
               
            })}

        </div>
    )
}
